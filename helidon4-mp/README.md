# Helidon 4 MicroProfile managed JPA

Helidon MP application using Jakarta REST, CDI, MicroProfile Health and
Telemetry, a named HikariCP data source, and container-managed Hibernate
JPA/JTA transactions.

```shell
./mvnw clean verify
java -jar target/helidon4-mp.jar
```
